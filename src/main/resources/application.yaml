spring:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_batch_fetch_size: 1000

logging:
  level:
    org.hibernate.sql: DEBUG
#    org.hibernate.type: TRACE
