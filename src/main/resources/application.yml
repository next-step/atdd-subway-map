spring:
  jpa:
    properties:
      hibernate.show_sql: true
      hibernate.format_sql: true
      default_batch_fetch_size: 1000

logging:
  level:
    org.hibernate:
      type:
        descriptor.sql: trace